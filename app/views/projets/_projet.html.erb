<h2><%= @prediction_date_fin %></h2>

<h3>Graphe de progression</h3>
<%= image_tag "http://chart.apis.google.com/chart?chs=520x200&cht=lxy&chd=t:0,#{@google_graph[:max_x]}|0,0|#{@google_graph[:data]}&chxt=x,y&chds=0,#{@google_graph[:max_x]},0,#{@google_graph[:max_y]},0,#{@google_graph[:max_x]},0,#{@google_graph[:max_y]},0,#{@google_graph[:max_x]},0,#{@google_graph[:max_y]}&chxr=0,0,#{@google_graph[:max_x]},5|1,0,#{@google_graph[:max_y]},5&chm=b,CCCCEE,0,1,0|b,EECCCC,1,2,0" %>
<p>Date de stabilisation du backlog : <%=l @date_stabilisation_backlog %></p>

<h3>Backlog</h3>

<%= link_to 'Ajouter une tâche', new_admin_projet_tache_path if mode_admin? %> |
<%= link_to 'Paramètres', edit_admin_projet_path if mode_admin? %> 

<table>
  <tr>
    <th>Id</th>
    <th>Date entrée</th>
    <th>Description</th>
    <th>Statut</th>
    <th>Date sortie</th>
    <th>Poids</th>
    <th>Commentaire</th>
  </tr>

<% @taches.each do |tache| %>
  <tr>
    <td><%=h tache.id %></td>
    <td><%=l tache.date_entree %></td>
    <td><%=h tache.description %></td>
    <td><%=h tache.statut %></td>
    <td><%=l tache.date_sortie if tache.date_sortie %></td>
    <td><%=h tache.poids %></td>
    <td><%=h tache.commentaire %></td>
    <td><%= link_to 'Show', admin_projet_tache_path(tache) if mode_admin? %></td>
    <td><%= link_to 'Edit', edit_admin_projet_tache_path(tache) if mode_admin? %></td>
    <td><%= link_to 'Destroy', admin_projet_tache_path(tache), :confirm => 'Are you sure?', :method => :delete if mode_admin? %></td>
  </tr>
<% end %>
</table>

<%= link_to 'Ajouter une tâche', new_admin_projet_tache_path if mode_admin? %> |
<%= link_to 'Paramètres', edit_admin_projet_path if mode_admin? %> 

